address: 0.0.0.0:7373
root:    disabled  

tls:
  key:  /build/kes-server.key
  cert: /build/kes-server.cert

log:
  error: on   
  audit: on   

policy:    
  minio-policy: 
    paths:
    - /v1/key/create/minio-key
    - /v1/key/generate/minio-key
    - /v1/key/decrypt/minio-key
    identities:
    - ${MINIO_IDENTITY}

keys:
  fs:
    path: ./keys
  # vault:
  #   endpoint: https://vault:8201
  #   approle:
  #     id:     "" # Your AppRole ID
  #     secret: "" # Your AppRole Secret ID
  #     retry:  15s
  #   status:
  #     ping: 10s
  #   tls:
  #     ca: /build/vault.crt # Since we use self-signed certificates